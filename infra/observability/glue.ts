// glue is meant to provide "centralized cataloging" of the structure of data at a given source.
// glue "datacatalog" that is manually specified, we do not epect log structure to change.
// research: is the one entity in the catalog sufficient? I see glue can (must?) use storage to keep results.
// so perhaps as well: glue database.
// ???
// *profit
