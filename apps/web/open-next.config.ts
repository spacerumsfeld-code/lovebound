const config = {
    default: {
        override: {
            wrapper: 'aws-lambda-streaming',
        },
        middleware: {
            external: true,
        },
    },
}

export default config
